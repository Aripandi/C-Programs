#include<stdio.h>

main()
{
    int number;

    printf("Enter the Number: ");
    //reads and stores the value in number
    scanf("%d",&number);

    //Print the value
    printf("You entered: %d",number);
}
